# Homeworks

Malte Fogde Mikkelsen
Student number: 202308803
Repository: https://github.com/maltefogde/PPNM

## Viewing the results

The numerical output from the latest compilations are written to `Out.txt`.
Also notice the generated plots that are saved as `.svg` files.

## Running the programs

Each homework folder contains its own `Makefile`.
To compile and run a homework, open a terminal in the relevant homework folder and run:

```bash
make clean
make
```

Note that some homeworks depend on the shared folder `lib`.

## AI usage

ChatGPT was used in all homeworks to discuss algorithms, debug code, improve structure, and clarify numerical or mathematical details.